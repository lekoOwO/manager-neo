# manager-neo

Rust-based local LLM manager: backend (axum/tokio/ratatui), TUI, CLI, and Vue 3 + PrimeVue frontend.

Progressive Disclosure: see `backend/`, `frontend/`, `docs/` for details. Use `manager-neo layout migrate` to migrate legacy compose flags.

License: GPL-3.0-or-later
